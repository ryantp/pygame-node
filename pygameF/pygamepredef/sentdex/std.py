#!python

# the SentDex Standard four functions (if i can remember them)

def text_objects():
	pass

message_to_screen(WIDTH, HEIGHT, msg, color, x_displace = 0, y_displace = 0, size = 'small'):
	pass

button(text, x, y, w, h, active_color, inactive_color, action):
	pass

text_to_button(msg):
	pass 